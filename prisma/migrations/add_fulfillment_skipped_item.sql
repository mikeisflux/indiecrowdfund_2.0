-- SUPERSEDED: this file was accidentally written in MySQL dialect and can
-- never execute on PostgreSQL. Its objects are created for real by
-- add_fulfillment_skipped_item_pg.sql. Left as a no-op so migration history
-- and the deploy runner's tracking stay intact.
SELECT 1;
